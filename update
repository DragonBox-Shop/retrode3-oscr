# update and rebase on latest upstream version

set -e

git checkout base
git pull sanni master

git checkout for-upstream
git rebase base || { git rebase --abort; git rebase -i base; }

git checkout master
git rebase for-upstream || { git rebase --abort; git rebase -i for-upstream; }
